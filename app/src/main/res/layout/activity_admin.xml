<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".AdminActivity">

    <Button
        android:id="@+id/users"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_margin="5dp"
        android:text="משתמשים" />

    <Button
        android:id="@+id/reports"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_below="@id/users"
        android:layout_margin="5dp"
        android:text="דוח תקלות" />

    <Button
        android:id="@+id/last_connected"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/reports"
        android:layout_centerInParent="true"
        android:layout_margin="5dp"
        android:text="דוח עובדים" />

    <Button
        android:id="@+id/create_worker"
        android:layout_width="300dp"
        android:layout_centerInParent="true"
        android:layout_height="wrap_content"
        android:layout_below="@id/last_connected"
        android:layout_margin="5dp"
        android:text="צור משתמש תפעולי" />

    <Button
        android:id="@+id/logout"
        android:layout_width="300dp"
        android:layout_centerInParent="true"
        android:layout_height="wrap_content"
        android:layout_below="@id/create_worker"
        android:layout_margin="5dp"
        android:text="התנתק" />


</RelativeLayout>